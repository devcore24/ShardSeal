global:
  scrape_interval: 15s
  evaluation_interval: 30s

scrape_configs:
  - job_name: 'shardseal'
    metrics_path: /metrics
    static_configs:
      - targets: ['localhost:8080']

# Optional: scrape Prometheus itself
# - job_name: 'prometheus'
#   static_configs:
#     - targets: ['localhost:9090']

alerting:
  alertmanagers:
    - static_configs:
        - targets: []  # e.g., ['localhost:9093']

rule_files:
  - rules.yml